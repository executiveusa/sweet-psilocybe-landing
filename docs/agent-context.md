# Agent Context Memory

## Project Overview
- **Name**: sweet-psilocybe-landing
- **Path**: `c:\Users\execu\Downloads\sweet-psilocybe-landing-main(1)\sweet-psilocybe-landing-main`
- **Stack**: Next.js 14, Tailwind CSS, TypeScript, React

## Recent Changes & Actions
- Restored missing project node dependencies using `npm install`.
- Started Next.js dev server listening on `http://localhost:3000`.
- Opened `http://localhost:3000` directly in default Windows web browser via `Start-Process`.
- Attempted `jcodemunch` indexing (restricted by path boundaries for default indexing scope).

## Run & Validation Commands
- Start dev server: `npm run dev`
- Local URL: `http://localhost:3000`

## Known Issues / Status
- Next.js dev server running on local machine.
- MCP DevTools Chrome debug listener port `9222` is unattached; system opened fallback browser directly via PowerShell `Start-Process`.
